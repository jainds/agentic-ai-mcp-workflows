[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "policy-service"
version = "1.0.0"
description = "Policy management service with FastAPI"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "prometheus-client",
    "PyJWT",
] 